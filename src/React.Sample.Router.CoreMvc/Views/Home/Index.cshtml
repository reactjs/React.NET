@using React.AspNet
@using React.Router
@using React.RenderFunctions

@{
	Layout = "_Layout";
}
@Html.ReactRouter("RootComponent", new { }, renderFunctions: new StyledComponentsFunctions(onPostRender: styles => ViewBag.ServerStyles = styles))
<script src="/components-bundle.generated.js"></script>
@Html.ReactInitJavaScript()
